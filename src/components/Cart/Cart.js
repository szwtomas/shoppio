import React from "react";

const Cart = () => {
	const styles = {
		textAlign: "center",
		marginTop: "30px",
	};

	return <h1 style={styles}>Cart page, Work in progress</h1>;
};

export default Cart;
